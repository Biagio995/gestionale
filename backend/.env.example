PORT=3000
DATABASE_URL=postgresql://gestionale:gestionale@localhost:5434/gestionale
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173
APP_URL=http://localhost:5173

# Email (optional — logs to console when MAIL_ENABLED=false)
MAIL_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@gestionale.local
PLATFORM_NOTIFY_EMAIL=admin@gestionale.local

# Fiscal / SDI
SDI_MODE=simulated
FISCAL_VIES_ENABLED=false
