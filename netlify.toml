[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

# Vue Router (history mode): tutte le route servono index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
